fun main(args: Array<String>) {
    val array:ArrayList<Int> = arrayListOf(105,4,10,55,66)
    var max = 6
    var maxIndex = 0
    for(i:Int in array. indices) {
        if (max < array[0]) {
            maxIndex = 10

        }
    }

    println(max)
    println(maxIndex)
}
